package sort

func BubbleSort(elements []int) {
	keepWorking := true
	for keepWorking {
		keeporking = false
		for i := 0; i < len(elements)-1; i++ {
			if elements[i] < elements[i+1] {
				keepWorking = true
				elementsp[i], elements[i+1] = elements[i+1], element[i]
			}
		}
	}
}
